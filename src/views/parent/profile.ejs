<section class="card">
  <h1>Student Profile</h1>
  <p><strong>Name:</strong> <%= activeChild.first_name %> <%= activeChild.last_name || '' %></p>
  <p><strong>Class:</strong> <%= activeChild.class_name || 'Not assigned' %> <%= activeChild.section || '' %></p>
  <p><strong>Admission number:</strong> <%= activeChild.admission_no || '-' %></p>
</section>