<%
    const l = (typeof lan !== 'undefined' && lan) ? lan : {};
%>
<!DOCTYPE html>
<html lang="en" data-theme="light">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="theme-color" content="#2563EB">
    <meta name="mobile-web-app-capable" content="yes">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="apple-mobile-web-app-status-bar-style" content="default">
    <meta name="apple-mobile-web-app-title" content="SchoolSync">
    <link rel="manifest" href="/manifest.json">
    <link rel="apple-touch-icon" href="/icons/icon-192.png">
    <link rel="icon" type="image/x-icon" href="/images/favicon.ico">
    <title><%= typeof title !== 'undefined' ? title : (l.Access_Denied_Title || '403 - Access Denied') %></title>
    <script src="/js/pwa.js" defer></script>
    <script src="https://unpkg.com/@lottiefiles/lottie-player@latest/dist/lottie-player.js"></script>
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #0f2027 0%, #203a43 50%, #2c5364 100%);
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            color: #ffffff;
        }

        .container {
            text-align: center;
            padding: 3rem 2rem;
            max-width: 600px;
            background: rgba(255, 255, 255, 0.05);
            backdrop-filter: blur(12px);
            -webkit-backdrop-filter: blur(12px);
            border-radius: 20px;
            border: 1px solid rgba(255, 255, 255, 0.1);
            box-shadow: 0 25px 45px rgba(0,0,0,0.2);
        }

        lottie-player {
            width: 260px;
            height: 260px;
            margin: 0 auto;
        }

        h1 {
            font-size: 2.8rem;
            margin: 1rem 0;
            color: #ff6b6b;
            text-shadow: 2px 2px 4px rgba(0,0,0,0.3);
        }

        p {
            font-size: 1.1rem;
            margin-bottom: 2.5rem;
            opacity: 0.9;
            line-height: 1.6;
        }

        .btn-group {
            display: flex;
            justify-content: center;
            gap: 15px;
            flex-wrap: wrap;
        }

        .btn {
            display: inline-block;
            padding: 12px 30px;
            background: #ff6b6b;
            color: #fff;
            text-decoration: none;
            border-radius: 50px;
            font-weight: 600;
            font-size: 1rem;
            transition: all 0.3s ease;
            box-shadow: 0 8px 20px rgba(255, 107, 107, 0.3);
            border: none;
            cursor: pointer;
        }

        .btn:hover {
            transform: translateY(-3px);
            box-shadow: 0 12px 25px rgba(255, 107, 107, 0.4);
            background: #ff5252;
        }

        .btn-outline {
            background: transparent;
            color: #fff;
            border: 2px solid rgba(255, 255, 255, 0.5);
            box-shadow: none;
        }

        .btn-outline:hover {
            background: rgba(255, 255, 255, 0.1);
            border-color: #fff;
        }

        .error-code {
            font-size: 1.2rem;
            opacity: 0.5;
            margin-top: 2rem;
            letter-spacing: 3px;
            font-family: monospace;
        }
        @media (max-width: 480px) {
            .container { padding: 2rem 1.5rem; }
            h1 { font-size: 2rem; }
            p { font-size: 1rem; margin-bottom: 1.5rem; }
            lottie-player { width: 180px; height: 180px; }
        }
    </style>
</head>
<body>
    <div class="container">
        <lottie-player src="https://assets4.lottiefiles.com/packages/lf20_tno6cgib.json" background="transparent" speed="1" loop autoplay></lottie-player>
        <h1><%- l.Access_Denied || 'Access Denied!' %></h1>
        <p><%= typeof message !== 'undefined' ? message : (l.Access_Denied_Desc || "Sorry, you don't have permission to access this page. Please check your credentials or contact the administrator.") %></p>
        <div class="btn-group">
            <a href="/" class="btn"><%- l.Back_To_Home || '← Back to Home' %></a>
            <a href="/login" class="btn btn-outline"><%- l.Login_Again || '🔒 Login Again' %></a>
        </div>
        <div class="error-code"><%- l.Error_Code || 'Error Code' %>: <%= typeof errorCode !== 'undefined' ? errorCode : '403' %></div>
    </div>
</body>
</html>